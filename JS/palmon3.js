function escolha() {
    while (true) {
        var caminho = prompt('Que golpe você quer usar? 1 - Usar soco supersônico, 2 - Rajada de espinhos');

        if (caminho == 1) {
            return location = ('../HTML/palmon4.html');

        } else if (caminho == 2) {
            return location = ('../HTML/palmon5.html');

        } else {
            alert('Escolha uma opção válida');
        }
    }
}