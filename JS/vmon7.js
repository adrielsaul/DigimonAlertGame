function escolha() {
    while (true) {
        var caminho = prompt('1 - Espada ou 2 - Arco e Flecha?');

        if (caminho == 1) {
            return location = ('../HTML/vmon9.html');

        } else if (caminho == 2) {
            return location = ('../HTML/vmon8.html');

        } else {
            alert('Escolha uma opção válida');
        }
    }
}