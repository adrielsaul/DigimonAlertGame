function escolha() {
    while (true) {
        var caminho = prompt('1 - Foge para a praia, ou 2 - Ajuda Tailmon a derrotar Andromon');

        if (caminho == 1) {
            return location = ('../HTML/palmon7.html');

        } else if (caminho == 2) {
            return location = ('../HTML/palmon6.html');

        } else {
            alert('Escolha uma opção válida');
        }
    }
}