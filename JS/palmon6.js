function escolha() {
    while (true) {
        var caminho = prompt('1 - Atacar, ou 2 - Usar digiovo');

        if (caminho == 1) {
            return location = ('../HTML/palmon8.html');

        } else if (caminho == 2) {
            return location = ('../HTML/palmon9.html');

        } else {
            alert('Escolha uma opção válida');
        }
    }
}