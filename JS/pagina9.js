function escolha() {
    while (true) {
        var caminho = prompt('Para avançar digite 1');

        if (caminho == 1) {
            return location = ('../HTML/pagina10.html');

        } else {
            alert('Escolha uma opção válida');
        }
    }
}