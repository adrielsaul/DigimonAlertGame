function escolha() {
    while (true) {
        var caminho = prompt('1 - Atacar wargreymon? 2 - Usar Digiovo?');

        if (caminho == 1) {
            return location = ('../HTML/pagina18.html');

        } else if (caminho == 2) {
            return location = ('../HTML/pagina19.html');

        } else {
            alert('Escolha uma opção válida');
        }
    }
}