function escolha() {
    while (true) {
        var caminho = prompt(' 1 - Faz um discurso emocionado com a intenção de que um milagre aconteça. 2 - Usa a poção com o anticorpo x');

        if (caminho == 1) {
            return location = ('../HTML/palmon11.html');

        } else if (caminho == 2) {
            return location = ('../HTML/palmon10.html');

        } else {
            alert('Escolha uma opção válida');
        }
    }
}