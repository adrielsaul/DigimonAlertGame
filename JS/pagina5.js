function escolha() {
    while (true) {
        var caminho = prompt(' 1 - Azul ou 2 - Rosa?');

        if (caminho == 1) {
            return location = ('../HTML/pagina14.html');

        } else if (caminho == 2) {
            return location = ('../HTML/pagina15.html');

        } else {
            alert('Escolha uma opção válida');
        }
    }
}